{
  "name": "howland-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Howland mobile (Capacitor) wrapper around the apps/app web build. Standalone npm project, intentionally outside the pnpm workspace.",
  "scripts": {
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "sync": "cap sync",
    "copy": "cap copy"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor/ios": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/assets": "3.0.5",
    "@capacitor/cli": "^7.4.0",
    "typescript": "^5.6.0"
  }
}
